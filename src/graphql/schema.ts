import * as mutationTypes from './mutations';
import * as queryTypes from './queries';
import * as objectTypes from './types';

const types = [mutationTypes, queryTypes, objectTypes];

export { types };
