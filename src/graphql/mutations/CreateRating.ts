import { mutationField, arg, inputObjectType } from 'nexus';
/*
const CreateRatingInput = inputObjectType({
    name: 'CreateRatingInput',
    definition(t) {
        
  });
  
  const CreateRating = mutationField('create_rating', {
    
    
  });
  
  export { CreateRatingInput, CreateRating };
  */