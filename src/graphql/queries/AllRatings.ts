/*import { queryField, inputObjectType, arg } from 'nexus';

const AllRatings = queryField('all_ratings', {
  type: 'Reviewer',
  resolve() {
    return 'It exist';
  }
});


const AllRatingsInput = inputObjectType({
  name: '',
  definition(t) {
    t.id('id', { required: true });
  }
});

export { AllRatings, AllRatingsInput };*/